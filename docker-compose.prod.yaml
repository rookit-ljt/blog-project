services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rookie-blog
    restart: always
    ports:
      - "${BLOG_PORT:-80}:80"
    environment:
      - TZ=${TZ:-Asia/Tokyo}
    labels:
      - "app=rookie-blog"
