services:
  blog:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rookie-blog
    restart: always
    ports:
      - "${BLOG_PORT:-80}:80"
    environment:
      - TZ=Asia/Shanghai
    labels:
      - "app=rookie-blog"
